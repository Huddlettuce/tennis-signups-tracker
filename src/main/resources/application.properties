# Firebase service account key file INSIDE the container (works with the Dockerfile we set up)
app.firebase.service-account-key-path=./firebase-service-account-key.json

# server.port=8080   # (optional)
