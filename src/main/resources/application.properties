# Firebase Configuration
# For local development, set the full path to your downloaded service account key JSON file.
# Example: app.firebase.service-account-key-path=C:/Users/hudson/firebase-service-account-key.json
# IMPORTANT: For deployment, use environment variables (GOOGLE_APPLICATION_CREDENTIALS) instead of hardcoding paths.
app.firebase.service-account-key-path = C:/Users/hudson/Documents/Arduino/projects/firebase-service-account-key.json.json

# Server Port (optional, default is 8080)
# server.port=8080

# Remove old JPA/Datasource properties 